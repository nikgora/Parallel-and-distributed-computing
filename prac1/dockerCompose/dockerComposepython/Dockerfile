 # Використовуємо офіційний образ Python зі списку доступних
FROM python:3.12
 # Встановлюємо робочу директорію в контейнері
WORKDIR /app
 # Копіюємо файли з вашого локального контексту (де знаходиться Dockerfile) в контейнер
COPY . /app
 # Встановлюємо залежності, якщо вони є у файлі requirements.txt
RUN pip install -r requirements.txt
 # Вказуємо команду для запуску вашого Python додатку при старті контейнера
CMD ["python", "main.py"]
